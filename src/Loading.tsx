import React from 'react';
import { Spin } from 'antd';

export default () => (
	<div style={{ paddingTop: 100, paddingBottom: 50, textAlign: 'center', backgroundColor: 'transparent' }}>
		<Spin size="large" />
	</div>
);
